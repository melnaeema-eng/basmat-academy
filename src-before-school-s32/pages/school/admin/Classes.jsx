import {useEffect,useState} from "react";import {getSchoolCore,saveClassSection} from "../../../services/schoolService";
const empty={academic_year_id:"",grade_level_id:"",curriculum_id:"",section_name:"A",capacity:"",is_active:true};
export default function Classes(){const[data,setData]=useState(null),[form,setForm]=useState(empty),[saving,setSaving]=useState(false);async function load(){const d=await getSchoolCore();setData(d);setForm(f=>({...f,academic_year_id:f.academic_year_id||d.years.find(x=>x.is_current)?.id||"",curriculum_id:f.curriculum_id||d.curricula[0]?.id||"",grade_level_id:f.grade_level_id||d.grades[0]?.id||""}))}useEffect(()=>{load()},[]);async function submit(e){e.preventDefault();try{setSaving(true);await saveClassSection(form);setForm(f=>({...f,section_name:"A",capacity:""}));await load()}catch(e){alert(e.message)}finally{setSaving(false)}}return <div><h1 className="text-3xl font-extrabold text-[#12345b]">الفصول الدراسية</h1><p className="mt-2 text-slate-500">إنشاء الفصول حسب العام والصف والمنهج.</p><div className="mt-6 grid gap-6 lg:grid-cols-[440px_1fr]"><form onSubmit={submit} className="academy-card space-y-4 p-5"><Field label="العام الدراسي"><select required className="academy-input" value={form.academic_year_id} onChange={e=>setForm({...form,academic_year_id:e.target.value})}><option value="">اختر</option>{(data?.years||[]).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field><Field label="المستوى"><select required className="academy-input" value={form.grade_level_id} onChange={e=>setForm({...form,grade_level_id:e.target.value})}>{(data?.grades||[]).map(x=><option key={x.id} value={x.id}>{x.name_ar}</option>)}</select></Field><Field label="المنهج"><select required className="academy-input" value={form.curriculum_id} onChange={e=>setForm({...form,curriculum_id:e.target.value})}>{(data?.curricula||[]).map(x=><option key={x.id} value={x.id}>{x.name_ar}</option>)}</select></Field><div className="grid gap-3 sm:grid-cols-2"><Field label="اسم الفصل"><input required className="academy-input" value={form.section_name} onChange={e=>setForm({...form,section_name:e.target.value})}/></Field><Field label="السعة"><input type="number" min="1" className="academy-input" value={form.capacity} onChange={e=>setForm({...form,capacity:e.target.value})}/></Field></div><button disabled={saving||!data?.years?.length} className="academy-btn-primary w-full">{saving?"جاري الحفظ...":"إنشاء الفصل"}</button>{data&&!data.years.length&&<p className="text-xs font-bold text-red-600">أضف عامًا دراسيًا أولًا.</p>}</form><div className="space-y-3">{(data?.sections||[]).map(x=><div key={x.id} className="academy-card flex flex-wrap items-center justify-between gap-3 p-5"><div><div className="font-extrabold text-[#12345b]">{x.school_grade_levels?.name_ar} — فصل {x.section_name}</div><div className="mt-1 text-sm text-slate-500">{x.school_curricula?.name_ar}</div></div><div className="text-sm font-bold text-orange-600">{x.capacity?`السعة ${x.capacity}`:"بدون حد"}</div></div>)}{data&&!data.sections.length&&<div className="academy-card p-8 text-center text-slate-500">لا توجد فصول بعد.</div>}</div></div></div>}
function Field({label,children}){return <label className="block"><span className="mb-1.5 block text-sm font-bold text-slate-700">{label}</span>{children}</label>}
