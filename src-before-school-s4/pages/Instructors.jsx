import {useEffect,useState} from "react";import {Link} from "react-router-dom";import {useTranslation} from "react-i18next";import MainLayout from "../layouts/MainLayout";import {getPublicInstructors} from "../services/professionalAcademyService";
export default function Instructors(){const{i18n}=useTranslation();const ar=i18n.language?.startsWith("ar");const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{getPublicInstructors().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);
 return <MainLayout><main className="min-h-screen bg-[#f7f9fc] py-10"><div className="academy-container"><span className="academy-eyebrow">{ar?"خبرات عملية":"Practical Expertise"}</span><h1 className="academy-title mt-3 text-3xl md:text-4xl">{ar?"مدربو الأكاديمية":"Academy Instructors"}</h1><p className="mt-3 max-w-3xl leading-8 text-slate-500">{ar?"خبراء في الاتصالات والتقنية والهندسة والجودة والتخطيط والاستشارات وELV.":"Experts in telecom, technology, engineering, quality, planning, consulting and ELV."}</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}{loading?<div className="academy-card mt-6 p-10 text-center">{ar?"جاري التحميل...":"Loading..."}</div>:<div className="mt-7 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{items.map(x=><Link key={x.id} to={`/instructors/${x.id}`} className="academy-card overflow-hidden hover:-translate-y-1 hover:shadow-lg"><img src={x.photo_url||"https://placehold.co/600x500?text=Instructor"} className="aspect-[4/3] w-full object-cover"/><div className="p-5"><h2 className="text-xl font-extrabold text-[#08284d]">{x.full_name}</h2><p className="mt-1 text-sm text-orange-600">{x.title}</p><div className="mt-4 border-t pt-3"><div className="mb-1 text-xs font-extrabold text-slate-400">{ar?"نبذة عن المدرب":"ABOUT THE INSTRUCTOR"}</div><p className="line-clamp-4 text-sm leading-7 text-slate-600">{x.bio|| (ar?"سيتم إضافة التعريف قريبًا.":"Bio will be added soon.")}</p></div><div className="mt-3 flex flex-wrap gap-2">{(x.specialties||[]).slice(0,4).map(s=><span key={s} className="rounded-full bg-slate-100 px-3 py-1 text-xs">{s}</span>)}</div></div></Link>)}</div>}</div></main></MainLayout>}
