SPRINT 11 — ADMIN SCROLL FIX
============================
Fixes missing vertical scrolling in the Admin dashboard/pages.

NO SQL.
Only AdminLayout.jsx is changed.

Install:
cd C:\projects\basmat-academy

Expand-Archive -Path "$env:USERPROFILE\Downloads\SPRINT-11-ADMIN-SCROLL-FIX.zip" -DestinationPath "C:\projects\basmat-academy" -Force

npm run dev

Test:
- /admin/dashboard
- /admin/students
- /admin/payments
- /admin/refunds
- /admin/email-outbox

Expected:
- Main Admin content scrolls vertically.
- Sidebar can also scroll if menu is taller than the screen.
- Mobile behavior remains normal.
