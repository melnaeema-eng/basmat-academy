import {useEffect,useState} from "react";import {getStudentPublishedResults} from "../../../services/schoolService";
export default function StudentResults(){const[data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{getStudentPublishedResults().then(setData).catch(e=>setError(e.message))},[]);return <div><h1 className="text-3xl font-extrabold text-[#12345b]">نتائجي</h1><p className="mt-2 text-slate-500">النتائج المنشورة وبطاقات الفصل الدراسي.</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="mt-6 grid gap-4 md:grid-cols-2">{(data?.results||[]).map(r=><div key={r.id} className="academy-card p-5"><div className="text-xs font-bold text-orange-600">{r.school_exams?.school_subjects?.name_ar} • {r.school_exams?.school_exam_periods?.name_ar}</div><h2 className="mt-1 font-extrabold text-[#12345b]">{r.school_exams?.title}</h2><div className="mt-4 text-3xl font-extrabold">{r.status==="present"?`${r.score} / ${r.school_exams?.max_score}`:r.status}</div></div>)}</div><h2 className="mt-8 text-2xl font-extrabold text-[#12345b]">بطاقات النتائج</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{(data?.cards||[]).map(c=><div key={c.id} className="academy-card p-5"><b>الفصل الدراسي {c.term_no}</b><div className="mt-3 text-4xl font-extrabold text-[#12345b]">{c.average_score??"—"}%</div><div className="mt-2 text-sm">{c.result_status}</div></div>)}</div></div>}
