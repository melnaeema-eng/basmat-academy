import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import CourseCard from './CourseCard';import {getCourses} from '../services/courseService';
export default function CourseSection(){const[c,setC]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{getCourses().then(x=>setC(x||[])).catch(e=>setError(e.message||'تعذر تحميل الدورات')).finally(()=>setLoading(false))},[]);return <section className="academy-section bg-[#f7f9fc]"><div className="academy-container" dir="rtl"><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><span className="academy-eyebrow">دورات مميزة</span><h2 className="academy-title mt-3 text-3xl">تعلم في المجال الذي يخدم مستقبلك</h2></div><Link to="/courses" className="font-extrabold text-[#16865a]">عرض جميع الدورات ←</Link></div>{loading?<div className="academy-card p-8 text-center">جاري التحميل...</div>:error?<div className="rounded-xl bg-red-50 p-5 text-red-700">{error}</div>:<div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{c.slice(0,6).map(x=><CourseCard key={x.id} course={x}/>)}</div>}</div></section>}
