import {useEffect,useState} from "react";import {Link} from "react-router-dom";import {useTranslation} from "react-i18next";import MainLayout from "../layouts/MainLayout";import {getLearningPaths} from "../services/professionalAcademyService";
export default function LearningPaths(){const{i18n}=useTranslation();const ar=i18n.language?.startsWith("ar");const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{getLearningPaths().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);
 return <MainLayout><main className="min-h-screen bg-[#f7f9fc] py-10"><div className="academy-container"><span className="academy-eyebrow">{ar?"تعلم بخطة واضحة":"Structured Learning"}</span><h1 className="academy-title mt-3 text-4xl">{ar?"المسارات التعليمية":"Learning Paths"}</h1><p className="mt-3 max-w-3xl leading-8 text-slate-500">{ar?"مسارات تجمع عدة دورات لتطوير مهارة أو تخصص بشكل متدرج.":"Structured paths that combine courses toward a clear professional goal."}</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}{loading?<div className="academy-card mt-6 p-10 text-center">{ar?"جاري التحميل...":"Loading..."}</div>:<div className="mt-7 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(p=><Link to={`/paths/${p.id}`} key={p.id} className="academy-card overflow-hidden hover:-translate-y-1 hover:shadow-lg"><img src={p.image_url||"https://placehold.co/800x450?text=Learning+Path"} className="aspect-video w-full object-cover"/><div className="p-5"><div className="text-xs font-bold text-orange-600">{p.level||"All Levels"}</div><h2 className="mt-2 text-xl font-extrabold text-[#08284d]">{p.title}</h2><p className="mt-2 line-clamp-3 text-sm leading-7 text-slate-500">{p.description}</p><div className="mt-4 font-bold text-slate-600">{p.learning_path_courses?.length||0} {ar?"دورة":"courses"}</div></div></Link>)}</div>}</div></main></MainLayout>}
