import {useEffect,useState} from "react";
import {useNavigate} from "react-router-dom";
import {getStudentAvailableOnlineExams,startSchoolOnlineExam} from "../../../services/schoolService";
export default function StudentOnlineExams(){const[rows,setRows]=useState([]);const nav=useNavigate();useEffect(()=>{getStudentAvailableOnlineExams().then(setRows).catch(e=>alert(e.message))},[]);async function start(id){try{const r=await startSchoolOnlineExam(id);nav(`/school/student/exam/${r.attempt_id}`)}catch(e){alert(e.message)}}return <div><h1 className="text-3xl font-extrabold text-[#12345b]">الامتحانات الإلكترونية</h1><div className="mt-5 space-y-3">{rows.map(x=><div key={x.id} className="academy-card flex flex-wrap items-center justify-between gap-4 p-5"><div><div className="text-xs font-bold text-orange-600">{x.school_subjects?.name_ar}</div><h2 className="font-extrabold text-[#12345b]">{x.title}</h2><div className="text-sm text-slate-500">{x.exam_date} • {x.duration_minutes} دقيقة</div></div><button onClick={()=>start(x.id)} className="academy-btn-primary">بدء الامتحان</button></div>)}</div></div>}
