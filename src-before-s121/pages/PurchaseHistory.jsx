import {useEffect,useState} from "react";import MainLayout from "../layouts/MainLayout";import {getOrders} from "../services/commerceService";
export default function PurchaseHistory(){const[rows,setRows]=useState([]);useEffect(()=>{getOrders().then(setRows)},[]);return <MainLayout><main className="min-h-screen bg-[#f7f9fc] py-10"><div className="academy-container max-w-5xl"><h1 className="academy-title text-3xl">المشتريات والفواتير</h1><div className="mt-6 space-y-4">{rows.map(o=><article key={o.id} className="academy-card p-5"><div className="flex flex-wrap justify-between gap-3"><div><div dir="ltr" className="font-extrabold text-[#08284d]">{o.invoice_number}</div><div className="text-xs text-slate-400">{new Date(o.created_at).toLocaleString()}</div></div><div dir="ltr" className="text-xl font-extrabold">{Number(o.total).toFixed(2)} {o.currency}</div></div><div className="mt-4 border-t pt-3">{o.order_items?.map(i=><div key={i.id} className="flex justify-between py-2"><span>{i.course_title}</span><span dir="ltr">{Number(i.final_amount).toFixed(2)} SAR</span></div>)}</div><button onClick={()=>window.print()} className="mt-3 rounded-xl border px-4 py-2 text-sm font-bold">طباعة / حفظ PDF</button></article>)}{!rows.length&&<div className="academy-card p-10 text-center">لا توجد مشتريات بعد.</div>}</div></div></main></MainLayout>}
