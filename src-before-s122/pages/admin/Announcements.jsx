import {useEffect,useState} from "react";import {useTranslation} from "react-i18next";import {supabase} from "../../services/supabase";import {AdminCard,AdminPageHeader} from "../../components/admin/AdminUI";import {adminGetAnnouncements,deleteAnnouncement,saveAnnouncement} from "../../services/finalProductionService";
export default function Announcements(){const{i18n}=useTranslation();const ar=i18n.language?.startsWith("ar");const[rows,setRows]=useState([]),[courses,setCourses]=useState([]),[form,setForm]=useState({course_id:"",title:"",body:"",is_published:true});async function load(){const[a,c]=await Promise.all([adminGetAnnouncements(),supabase.from("courses").select("id,title").order("title")]);if(c.error)throw c.error;setRows(a);setCourses(c.data||[])}useEffect(()=>{load()},[]);async function submit(e){e.preventDefault();await saveAnnouncement(form);setForm({course_id:"",title:"",body:"",is_published:true});await load()}return <div><AdminPageHeader title={ar?"إعلانات الدورات":"Course Announcements"}/><div className="grid gap-6 xl:grid-cols-[420px_1fr]"><AdminCard className="p-5"><form onSubmit={submit} className="space-y-4"><select required className="academy-input" value={form.course_id} onChange={e=>setForm({...form,course_id:e.target.value})}><option value="">— {ar?"اختر الدورة":"Choose course"} —</option>{courses.map(c=><option key={c.id} value={c.id}>{c.title}</option>)}</select><input required className="academy-input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder={ar?"العنوان":"Title"}/><textarea required className="academy-input min-h-32" value={form.body} onChange={e=>setForm({...form,body:e.target.value})} placeholder={ar?"نص الإعلان":"Announcement"}/><button className="academy-btn-primary w-full">{ar?"نشر":"Publish"}</button></form></AdminCard><div className="space-y-4">{rows.map(x=><AdminCard key={x.id} className="p-5"><div className="text-xs font-bold text-orange-600">{x.courses?.title}</div><h3 className="mt-1 font-extrabold text-[#08284d]">{x.title}</h3><p className="mt-2 line-clamp-3 text-sm text-slate-600">{x.body}</p><button onClick={async()=>{if(confirm(ar?"حذف الإعلان؟":"Delete announcement?")){await deleteAnnouncement(x.id);await load()}}} className="mt-4 rounded-lg bg-red-50 px-3 py-2 text-sm font-bold text-red-600">{ar?"حذف":"Delete"}</button></AdminCard>)}</div></div></div>}
