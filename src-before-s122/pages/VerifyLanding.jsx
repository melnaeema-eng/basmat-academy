import {useState} from "react";import {useNavigate} from "react-router-dom";import {useTranslation} from "react-i18next";import MainLayout from "../layouts/MainLayout";
export default function VerifyLanding(){const nav=useNavigate();const{i18n}=useTranslation();const ar=i18n.language?.startsWith("ar");const[token,setToken]=useState("");function go(e){e.preventDefault();if(token.trim())nav(`/verify/${encodeURIComponent(token.trim())}`)}return <MainLayout><main className="min-h-[65vh] bg-[#f7f9fc] py-14"><div className="academy-container max-w-2xl"><div className="academy-card p-7 text-center md:p-10"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-emerald-50 text-3xl">✓</div><h1 className="academy-title mt-4 text-3xl">{ar?"التحقق من الشهادة":"Verify Certificate"}</h1><p className="mt-3 leading-8 text-slate-500">{ar?"أدخل رمز التحقق الموجود في الشهادة أو استخدم QR Code مباشرة.":"Enter the verification token printed on the certificate or scan its QR code."}</p><form onSubmit={go} className="mt-6 flex gap-2"><input dir="ltr" required value={token} onChange={e=>setToken(e.target.value)} className="academy-input" placeholder="Verification token"/><button className="academy-btn-primary shrink-0">{ar?"تحقق":"Verify"}</button></form></div></div></main></MainLayout>}
