import {useEffect,useState} from "react";import {useTranslation} from "react-i18next";import {FaBookOpen,FaClock,FaGraduationCap,FaUsers} from "react-icons/fa";import {supabase} from "../../services/supabase";import {AdminCard,AdminPageHeader} from "../../components/admin/AdminUI";
export default function Dashboard(){const{t}=useTranslation();const[stats,setStats]=useState({students:0,courses:0,enrollments:0,pendingPayments:0}),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){try{setLoading(true);setError('');const[p,c,e,pay]=await Promise.all([supabase.from('profiles').select('id',{count:'exact',head:true}).neq('role','admin'),supabase.from('courses').select('id',{count:'exact',head:true}),supabase.from('enrollments').select('id',{count:'exact',head:true}),supabase.from('payments').select('id',{count:'exact',head:true}).eq('status','pending')]);for(const r of[p,c,e,pay])if(r.error)throw r.error;setStats({students:p.count||0,courses:c.count||0,enrollments:e.count||0,pendingPayments:pay.count||0})}catch(e){setError(e.message||'Dashboard error')}finally{setLoading(false)}}useEffect(()=>{load()},[]);const cards=[[t('admin.totalStudents'),stats.students,FaUsers,'bg-orange-50 text-orange-600'],[t('admin.totalCourses'),stats.courses,FaBookOpen,'bg-emerald-50 text-emerald-600'],[t('admin.totalEnrollments'),stats.enrollments,FaGraduationCap,'bg-blue-50 text-blue-600'],[t('admin.pendingPayments'),stats.pendingPayments,FaClock,'bg-amber-50 text-amber-600']];return <div><AdminPageHeader title={t('admin.dashboard')} description={t('admin.dashboardDesc')} actions={<button onClick={load} className="academy-btn-primary">{loading?t('common.loading'):t('common.refresh')}</button>}/>{error&&<div className="mb-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon,cls])=><AdminCard key={label} className="p-5"><div className="flex items-center justify-between gap-4"><div><div className="text-sm font-semibold text-slate-500">{label}</div><div className="mt-2 text-4xl font-extrabold text-[#08284d]">{loading?'…':value}</div></div><div className={`flex h-12 w-12 items-center justify-center rounded-2xl ${cls}`}><Icon className="text-xl"/></div></div></AdminCard>)}</div></div>}
