import {useEffect,useState} from "react";import {useTranslation} from "react-i18next";import MainLayout from "../layouts/MainLayout";import {supabase} from "../services/supabase";import {Directional,StatusBadge} from "../components/admin/AdminUI";
export default function MyPayments(){const{t}=useTranslation();const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{(async()=>{try{const{data:{session}}=await supabase.auth.getSession();if(!session?.user)return;const{data,error}=await supabase.from('payments').select('*,courses(id,title)').eq('user_id',session.user.id).order('created_at',{ascending:false});if(error)throw error;setItems(data||[])}catch(e){setError(e.message)}finally{setLoading(false)}})()},[]);return <MainLayout><main className="min-h-screen bg-[#f7f9fc] py-10"><div className="academy-container max-w-5xl"><h1 className="academy-title text-3xl">{t('studentPages.payments')}</h1><p className="mt-2 text-slate-500">{t('studentPages.paymentsDesc')}</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="mt-6 space-y-4">{loading?<div className="academy-card p-10 text-center">{t('common.loading')}</div>:items.map(p=><div key={p.id} className="academy-card p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="font-extrabold text-[#08284d]">{p.courses?.title||p.course_id}</h2><div className="mt-2 text-sm text-slate-500"><Directional>{Number(p.amount).toFixed(2)} {p.currency}</Directional></div></div><StatusBadge status={p.status}/></div>{p.status==='rejected'&&<div className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{p.admin_note||t('common.rejected')}</div>}</div>)}{!loading&&!items.length&&<div className="academy-card p-10 text-center text-slate-500">{t('common.noResults')}</div>}</div></div></main></MainLayout>}
