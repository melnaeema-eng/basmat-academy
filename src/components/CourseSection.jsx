import {useEffect,useState} from "react";import {Link} from "react-router-dom";import CourseCard from "./CourseCard";import {getCourses} from "../services/courseService";
export default function CourseSection(){const[courses,setCourses]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{(async()=>{try{setCourses(await getCourses()||[])}catch(e){setError(e.message||'تعذر تحميل الدورات')}finally{setLoading(false)}})()},[]);return <section className="academy-section bg-[#f8fafc]"><div className="academy-container" dir="rtl"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><span className="academy-eyebrow">الدورات المميزة</span><h2 className="academy-title mt-3 text-3xl md:text-4xl">ابدأ بمسار يناسب هدفك</h2><p className="mt-2 text-slate-500">دورات تقنية عملية بتصميم واضح ومحتوى منظم.</p></div><Link to="/courses" className="font-bold text-[#0a2540] hover:text-[#f97316]">عرض جميع الدورات ←</Link></div>{loading&&<div className="academy-card p-8 text-center text-slate-500">جاري تحميل الدورات...</div>}{error&&<div className="rounded-2xl bg-red-50 p-5 text-red-700">{error}</div>}{!loading&&!error&&!courses.length&&<div className="academy-card p-8 text-center text-slate-500">لا توجد دورات منشورة حاليًا.</div>}{!loading&&!error&&courses.length>0&&<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{courses.slice(0,6).map(c=><CourseCard key={c.id} course={c}/>)}</div>}</div></section>}
