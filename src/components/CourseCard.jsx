import {useTranslation} from "react-i18next";import {useNavigate} from "react-router-dom";
export default function CourseCard({course}){const {t,i18n}=useTranslation(),navigate=useNavigate(),ar=i18n.language?.startsWith('ar');const title=(ar?course.title_ar:course.title_en)||course.title||t('course.untitled');const category=(ar?course.category_ar:course.category_en)||course.category||'';return <article className="academy-card group overflow-hidden hover:-translate-y-1 hover:shadow-xl"><div className="relative overflow-hidden"><img src={course.image||'https://placehold.co/800x450?text=Course'} alt={title} className="academy-course-image transition duration-500 group-hover:scale-[1.03]"/><span className="absolute top-3 right-3 rounded-full bg-white/95 px-3 py-1 text-xs font-bold text-[#071d49] shadow">{category||'Academy'}</span></div><div className="p-5"><h3 className="line-clamp-2 min-h-14 text-xl font-extrabold text-[#071d49]">{title}</h3>{course.instructor&&<p className="mt-2 text-sm text-slate-500">المدرب: {course.instructor}</p>}<div className="mt-5 flex items-center justify-between gap-3 border-t pt-4"><span className="font-extrabold text-[#ff7417]">{Number(course.price)>0?`${course.price} ${t('course.currency')}`:t('course.free')}</span><button onClick={()=>navigate(`/courses/${course.id}`)} className="rounded-xl bg-[#071d49] px-4 py-2 text-sm font-bold text-white hover:bg-[#0b2d70]">{t('course.details')}</button></div></div></article>}
