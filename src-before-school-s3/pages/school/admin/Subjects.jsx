import {useEffect,useState} from "react";import {getSchoolCore,saveSchoolSubject} from "../../../services/schoolService";
const empty={code:"",name_ar:"",name_en:"",description_ar:"",description_en:"",is_active:true};
export default function Subjects(){const[data,setData]=useState(null),[form,setForm]=useState(empty),[saving,setSaving]=useState(false);async function load(){setData(await getSchoolCore())}useEffect(()=>{load()},[]);async function submit(e){e.preventDefault();try{setSaving(true);await saveSchoolSubject(form);setForm(empty);await load()}catch(e){alert(e.message)}finally{setSaving(false)}}return <div><h1 className="text-3xl font-extrabold text-[#12345b]">المواد الدراسية</h1><p className="mt-2 text-slate-500">أضف المواد الأساسية أولًا، ثم سيتم توزيعها على الصفوف والمناهج.</p><div className="mt-6 grid gap-6 lg:grid-cols-[420px_1fr]"><form onSubmit={submit} className="academy-card space-y-4 p-5"><Field label="رمز المادة"><input required className="academy-input" placeholder="MATH" value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/></Field><Field label="اسم المادة بالعربية"><input required className="academy-input" value={form.name_ar} onChange={e=>setForm({...form,name_ar:e.target.value})}/></Field><Field label="اسم المادة بالإنجليزية"><input required className="academy-input" value={form.name_en} onChange={e=>setForm({...form,name_en:e.target.value})}/></Field><button disabled={saving} className="academy-btn-primary w-full">{saving?"جاري الحفظ...":"إضافة المادة"}</button></form><div className="grid gap-3 md:grid-cols-2">{(data?.subjects||[]).map(x=><div key={x.id} className="academy-card p-5"><div dir="ltr" className="text-xs font-bold text-orange-600">{x.code}</div><h2 className="mt-1 font-extrabold text-[#12345b]">{x.name_ar}</h2><div className="text-sm text-slate-500">{x.name_en}</div></div>)}{data&&!data.subjects.length&&<div className="academy-card p-8 text-center text-slate-500 md:col-span-2">لم تتم إضافة مواد بعد.</div>}</div></div></div>}
function Field({label,children}){return <label className="block"><span className="mb-1.5 block text-sm font-bold text-slate-700">{label}</span>{children}</label>}
