import {useEffect,useState} from "react";import {FaBookOpen,FaClipboardCheck,FaUserGraduate} from "react-icons/fa";import {getStudentSchoolDashboard,submitSchoolAssignment} from "../../../services/schoolService";
export default function StudentSchoolDashboard(){const[data,setData]=useState(null),[error,setError]=useState(""),[drafts,setDrafts]=useState({});async function load(){try{setData(await getStudentSchoolDashboard())}catch(e){setError(e.message)}}useEffect(()=>{load()},[]);async function submit(a){const text=drafts[a.id]||"";if(!text.trim())return alert("اكتب الإجابة أو الملاحظات");try{await submitSchoolAssignment({assignment_id:a.id,enrollment_id:data.enrollment_id,submission_text:text});alert("تم تسليم الواجب");await load()}catch(e){alert(e.message)}}const at=data?.attendance||{};return <div><div className="rounded-[28px] bg-[#12345b] p-7 text-white"><FaUserGraduate className="text-3xl text-orange-300"/><h1 className="mt-3 text-3xl font-extrabold">بوابة الطالب</h1><p className="mt-2 text-slate-200">واجباتك وحضورك ومتابعتك الدراسية.</p></div>{error&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="mt-6 grid gap-4 sm:grid-cols-4">{[["حاضر",at.present||0,"emerald"],["غائب",at.absent||0,"red"],["متأخر",at.late||0,"amber"],["بعذر",at.excused||0,"blue"]].map(([l,v])=><div key={l} className="academy-card p-5"><div className="text-sm text-slate-500">{l}</div><div className="mt-2 text-3xl font-extrabold text-[#12345b]">{v}</div></div>)}</div><h2 className="mt-8 text-2xl font-extrabold text-[#12345b]">الواجبات</h2><div className="mt-4 space-y-4">{(data?.homework||[]).map(a=><div key={a.id} className="academy-card p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-xs font-bold text-orange-600">{a.subject_name_ar}</div><h3 className="mt-1 text-lg font-extrabold text-[#12345b]">{a.title}</h3><p className="mt-2 text-sm leading-7 text-slate-600">{a.description||"—"}</p></div><div className="text-end text-xs text-slate-500">الدرجة: {a.score??`— / ${a.max_score}`}</div></div>{a.submitted_at?<div className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-700">تم التسليم {a.teacher_feedback&&`• ملاحظة المعلم: ${a.teacher_feedback}`}</div>:<div className="mt-4"><textarea className="academy-input min-h-24" placeholder="اكتب إجابتك أو ملاحظتك..." value={drafts[a.id]||""} onChange={e=>setDrafts({...drafts,[a.id]:e.target.value})}/><button onClick={()=>submit(a)} className="academy-btn-primary mt-2">تسليم الواجب</button></div>}</div>)}{data&&!data.homework?.length&&<div className="academy-card p-8 text-center text-slate-500">لا توجد واجبات حاليًا.</div>}</div></div>}
