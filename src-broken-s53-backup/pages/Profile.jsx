import {useEffect,useState} from "react";import {Navigate} from "react-router-dom";import {useTranslation} from "react-i18next";import MainLayout from "../layouts/MainLayout";import {getMyProfile,updateMyProfile,uploadAvatar} from "../services/profileService";
export default function Profile(){const{t}=useTranslation();const[form,setForm]=useState({full_name:"",email:"",phone:"",city:"",bio:"",avatar_url:""}),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[login,setLogin]=useState(false),[error,setError]=useState("");
 useEffect(()=>{(async()=>{try{const p=await getMyProfile();if(!p){setLogin(true);return}setForm({full_name:p.full_name||"",email:p.email||p.auth_email||"",phone:p.phone||"",city:p.city||"",bio:p.bio||"",avatar_url:p.avatar_url||""})}catch(e){setError(e.message)}finally{setLoading(false)}})()},[]);
 if(login)return <Navigate to="/login" replace/>;async function save(e){e.preventDefault();try{setSaving(true);await updateMyProfile(form);alert(t("common.save"))}catch(e){alert(e.message)}finally{setSaving(false)}}async function avatar(e){const f=e.target.files?.[0];if(!f)return;try{const url=await uploadAvatar(f);setForm(p=>({...p,avatar_url:url}));await updateMyProfile({...form,avatar_url:url})}catch(e){alert(e.message)}}
 return <MainLayout><main className="min-h-screen bg-[#f7f9fc] py-10"><div className="academy-container max-w-5xl">{loading?<div className="academy-card p-10 text-center">{t("common.loading")}</div>:<><div className="mb-6"><h1 className="academy-title text-3xl">{t("studentPages.profile")}</h1><p className="mt-2 text-slate-500">{t("studentPages.profileDesc")}</p></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<form onSubmit={save} className="academy-card p-5 md:p-7"><div className="flex flex-col gap-5 border-b border-slate-100 pb-6 sm:flex-row sm:items-center"><img src={form.avatar_url||"https://placehold.co/160x160?text=User"} className="h-24 w-24 rounded-full border object-cover"/><div><input type="file" accept="image/*" onChange={avatar} className="text-sm"/><p className="mt-2 text-xs text-slate-400">PNG / JPG</p></div></div><div className="mt-6 grid gap-4 md:grid-cols-2"><Field label={t("common.name")} value={form.full_name} onChange={v=>setForm({...form,full_name:v})}/><Field label={t("common.email")} value={form.email} disabled dir="ltr"/><Field label={t("common.phone")} value={form.phone} onChange={v=>setForm({...form,phone:v})} dir="ltr"/><Field label={t("common.city")} value={form.city} onChange={v=>setForm({...form,city:v})}/></div><label className="mt-4 block"><span className="mb-1.5 block text-sm font-bold">Bio</span><textarea className="academy-input min-h-32" value={form.bio} onChange={e=>setForm({...form,bio:e.target.value})}/></label><div className="mt-6"><button disabled={saving} className="academy-btn-primary">{saving?t("common.loading"):t("common.save")}</button></div></form></>}</div></main></MainLayout>}
function Field({label,value,onChange,disabled,dir}){return <label><span className="mb-1.5 block text-sm font-bold text-slate-700">{label}</span><input className="academy-input" dir={dir} disabled={disabled} value={value} onChange={e=>onChange?.(e.target.value)}/></label>}
