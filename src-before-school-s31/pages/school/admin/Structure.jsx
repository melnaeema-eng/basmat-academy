import {useEffect,useMemo,useState} from "react";import {getSchoolCore} from "../../../services/schoolService";
export default function Structure(){const[data,setData]=useState(null);useEffect(()=>{getSchoolCore().then(setData).catch(e=>alert(e.message))},[]);const grouped=useMemo(()=>{const m={};for(const s of data?.stages||[])m[s.id]={...s,grades:[]};for(const g of data?.grades||[])if(m[g.stage_id])m[g.stage_id].grades.push(g);return Object.values(m).sort((a,b)=>a.sort_order-b.sort_order)},[data]);return <div><h1 className="text-3xl font-extrabold text-[#12345b]">المراحل والصفوف</h1><p className="mt-2 text-slate-500">الهيكل الدراسي المعتمد: 15 مستوى دراسيًا.</p><div className="mt-7 grid gap-5 lg:grid-cols-2">{grouped.map(s=><section key={s.id} className="academy-card overflow-hidden"><div className="bg-[#12345b] px-5 py-4 text-white"><h2 className="text-xl font-extrabold">{s.name_ar}</h2><div className="text-xs text-slate-300">{s.name_en}</div></div><div className="grid gap-3 p-5 sm:grid-cols-2">{s.grades.map(g=><div key={g.id} className="rounded-xl border bg-slate-50 p-4"><div className="font-extrabold text-[#12345b]">{g.name_ar}</div><div className="mt-1 text-xs text-slate-500">{g.name_en}</div><div dir="ltr" className="mt-2 text-xs font-bold text-orange-600">{g.code}</div></div>)}</div></section>)}</div><div className="mt-6 academy-card p-5"><h2 className="text-xl font-extrabold text-[#12345b]">المناهج</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{(data?.curricula||[]).map(c=><div key={c.id} className="rounded-xl border bg-slate-50 p-4"><div className="font-extrabold">{c.name_ar}</div><div className="mt-1 text-sm text-slate-500">{c.name_en}</div><div className="mt-2 text-xs font-bold text-orange-600">لغة التدريس: {c.instruction_language==="ar"?"العربية":"English"}</div></div>)}</div></div></div>}
